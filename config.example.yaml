# Copy to config.yaml and fill in your values.
# Set DEEPGRAM_API_KEY as an environment variable.
output_dir: /data/transcriptions

whitelist:
  - name: Alice
    number: "447700900000"
    lid: "276909493588153"    # optional, for contacts using WhatsApp LID format
  - name: Bob
    number: "447700900001"
